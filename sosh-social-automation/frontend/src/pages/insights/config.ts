// Feature flag for insights mock data
// Set to true to use mock data, false to use real API
export const USE_INSIGHTS_MOCK = true;

// Mock auth state for testing
export const MOCK_AUTH_STATE = {
  twitter: true,
  instagram: true,
  tiktok: true,
  youtube: true,
};
